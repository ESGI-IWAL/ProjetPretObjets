spring:
  application:
    name: pretemoica
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/projetPret
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: localhost
    port: 1025
jwt:
  secret: pretemoica-dev-secret-key-pretemoica-dev-secret-key-123456
  expiration-ms: 3600000

