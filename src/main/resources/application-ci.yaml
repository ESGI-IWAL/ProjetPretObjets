spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: admin
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 3600000
